each constructor and method must have a unique signature.


However, in methods, we can have methods with the same name.. but a different signature. 

That means, different # of parameters and different types. 		